<div class="container" style="margin-top:70px;">
    <div class="row mt-5">
        <h3 class="text-center text-uppercase p-2">Lista de Canciones</h3>

        <div style="padding: 10px;">
            <button class="btn btn-danger">
                <a class="nav-link link-light" aria-current="page" href="/can_add"> Nuevos CANCIONES</a>
            </button>
        </div>

        {{#if canciones}}
        <table class="table text-center">
            <thead>
                <tr class="table-dark">
                    <th scope="col">ID</th>
                    <th scope="col">Producto</th>
                    <th scope="col">Titulo</th>
                    <th scope="col">Duracion</th>
                    <th scope="col">Acciones</th>
                </tr>
            </thead>
            <tbody>
                {{#each canciones}}
                <tr>
                    <td>{{id}}</td>
                    <!-- Cambia id_producto a nombre_producto -->
                    <td>{{nombre_producto}}</td>
                    <td>{{titulo}}</td>
                    <td>{{duracion}}</td>
                    <td>
                        <div>
                            <a href="/can_edit/{{id}}" class="btn btn-warning p-2">
                                <i class="fa-solid fa-pencil"></i> Actualizar
                            </a>
                            <a href="/can_delete/{{id}}" class="btn btn-danger p-2">
                                <i class="fa-solid fa-trash"></i> Eliminar
                            </a>
                        </div>
                    </td>
                </tr>
                {{/each}}
            </tbody>
            <tfoot>
                <tr class="table-dark">
                    <td colspan="10">REGISTRO DE CANCIONES</td>
                </tr>
            </tfoot>
        </table>
        {{else}}
        <div class="card text-center mx-auto" style="width: 350px;">
            <div class="card-header">
                <h3>No hay canciones disponibles</h3>
            </div>
            <div class="card-body">
                <p>Presiona este botón para agregar una nueva canción a la tienda.</p>
                <a href="/can_add" class="btn btn-primary">Ir a crear</a>
            </div>
        </div>
        {{/if}}
    </div>
</div>
